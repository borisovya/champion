nelmio_api_doc:
    documentation:
        info:
            title: Champion shop API
            description: API for Champion shop
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api/v1
